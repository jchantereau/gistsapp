<%= simple_form_for(@gist) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :content %>
  <%= f.input :category, collection: @categories %>
  <%= f.button :submit %>
<% end %>
