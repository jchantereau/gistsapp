<%= simple_form_for [@category, @gist] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :content %>
  <%= f.input :category_id, as: :select, collection: Category.all %>
  <%= f.button :submit %>
<% end %>
